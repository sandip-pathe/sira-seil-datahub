# Documentation uses its own review and link checks.
docs/**

# Frozen contracts, deterministic fixtures, generated clients, and lockfiles are
# produced or verified by their dedicated generators rather than reformatted.
contracts/**
fixtures/**
packages/api-client/src/**
pnpm-lock.yaml
uv.lock

# Local dependency and build output.
node_modules/**
.next/**
out/**
.venv/**
